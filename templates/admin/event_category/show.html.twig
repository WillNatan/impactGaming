{% extends 'admin/base.html.twig' %}

{% block title %}EventCategory{% endblock %}

{% block body %}
    <h1>EventCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_category.id }}</td>
            </tr>
            <tr>
                <th>CategoryName</th>
                <td>{{ event_category.categoryName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('event_category_index') }}">back to list</a>

    <a href="{{ path('event_category_edit', {'id': event_category.id}) }}">edit</a>

    {{ include('event_category/_delete_form.html.twig') }}
{% endblock %}
